- This cannot be captured in an image, but basically, when you send a message and that agent answers, it has two stages:
1. The answer when it's there
2. There's this settle stage where the UI kind of refreshes, and then we get the feedback pop-up and we get the different bubbles  That, to me, is just a jarring experience. The answer seems done, but then it has another kind of loading state or another refresh. That's very explicit, and I think it should change.
- I wonder if we can teach the model to render tables in a carousel. Instead of having the model have to layer in many tables after and after and after one another, it could layer them in in a carousel. I think we could give it the option to do both, potentially. As ways to optimize the UI, it'd be kind of nice to build this carousel functionality when the model has multiple tables it wants to render, because it's in a carousel format; it is just much sleeker. When you click on a citation, maybe the UI could flip to that carousel and then show the cell or that table, right? That could be sort of the UI element. I think, obviously, if you click on the citation and the table is all the way up, it should scroll up to the table, all that sort of stuff.
- We also see that we have this details dropdown. I'm wondering, instead of a details dropdown, if we can do a toggle between the details and the table itself. That way, when you click on the details dropdown, we're not just pushing the table even further down. You're sort of more swapping the main view. That expand button only shows when the table is rendered, when the table is the one that's toggled.
- You know, for the additional sources, I'm wondering if that is placed on the same line as the copy button.
- I'm wondering if when the user presses copy, they could also copy the actual table itself in a Markdown format, the same format that maybe the model gets.
- When we do our proposed table or proposed derived run, that table card should be after the message that the AI provides. It's weird to have the user interact with a table that's before the message that the AI says, right? You would think that you would lead into, "Below is a table; please press confirm to blah, blah, blah, blah." It just doesn't feel like the right chain, and it's really less about where it goes and more about the change should make sense, right? If I ask a question, then obviously the thinking tokens should be shown first, then the derived table, then this sort of actual message. I just think that sort of ordering should be more natural. When the answer is submitted, the UI doesn't update unless I click refresh, and when I click refresh then everything gets updated, so I just think that whole workflow needs to be much smoother. The auto confirm or the auto does work, but I think what's happening is that, in the background, we have this auto. Effectively, on the front end, we say the answer is done once I press confirm, but for this workflow that confirm maybe it's still seen as the model's response. Maybe it's about decoupling that little model response a bit further to be some other abstraction, right, so we get kind of better key in on the updates of the system.
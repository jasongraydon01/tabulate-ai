/**
 * @deprecated Pricing and demo are now production surfaces.
 * This helper remains only for backward compatibility and always returns `true`.
 */
export function isPreviewFeatureEnabled(): boolean {
  return true;
}
